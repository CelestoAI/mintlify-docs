---
title: "Introduction"
description: "Run code, start a browser, and give AI agents an isolated workspace with SmolVM"
---

# SmolVM

SmolVM gives AI agents their own disposable computer. Each microVM boots in milliseconds, runs any code or software you throw at it, keeps state when you need it, and vanishes when you don't — nothing touches your host.

## What you can do

<CardGroup cols={3}>
  <Card title="Run code safely" icon="code">
    Execute shell commands or scripts in a clean sandbox instead of on your host machine
  </Card>

  <Card title="Start a browser" icon="globe">
    Launch a real Chromium session you can automate with Playwright or watch live through noVNC
  </Card>

  <Card title="Power your agents" icon="robot">
    Plug SmolVM into any agent framework — Claude, PydanticAI, OpenAI Agents, LangChain, and more
  </Card>
</CardGroup>

## Quick example

```python
from smolvm import SmolVM

vm = SmolVM()
result = vm.run("echo 'Hello from the sandbox'")
print(result.stdout.strip())
vm.stop()
```

## Where to start

| What you want to do | Where to go |
| --- | --- |
| Run your first sandbox | [Quickstart](/smolvm/quickstart) |
| Build an agent with a sandboxed shell | [AI agent integration](/smolvm/guides/ai-agent-integration) |
| Start a disposable browser session | [Browser sessions](/smolvm/guides/ai-agent-integration#browser-sessions) |
| Let a model click and type on websites | [Computer-use with OpenAI](/smolvm/guides/ai-agent-integration#computer-use-with-openai) |
| Pass configuration into a sandbox | [Environment variables](/smolvm/guides/environment-variables) |
| Save and restore sandbox state | [Snapshots](/smolvm/guides/snapshots) |

## SDK or CLI?

Use the **SDK** when SmolVM is part of your application or agent loop and you need to create sandboxes from Python code.

Use the **CLI** when you want to manage sandboxes from the terminal, start a browser session, or script local workflows:

```bash
smolvm create                    # spin up a sandbox
smolvm ssh my-sandbox            # open a shell inside it
smolvm list                      # see all running sandboxes
smolvm stop my-sandbox           # tear it down
```

See the [CLI reference](/smolvm/cli/overview) for the full command list.

### Start a sandboxed browser from the CLI

You can launch an isolated browser session directly from the terminal. This is useful for quick testing, scripting, or handing the CDP endpoint to an external tool.

```bash
smolvm browser start --live --json
```

The JSON response includes a `session_id` and local browser URLs. Use the session ID to manage the session:

```bash
smolvm browser list
smolvm browser stop <session_id>
```

The `cdp_url` in the response can be passed to any Chrome DevTools Protocol client. For example, the [PydanticAI agent-browser integration](/smolvm/guides/ai-agent-integration#pydanticai-with-agent-browser) extracts the localhost port from that URL and hands it to `agent-browser --cdp`.

To open the live browser view in your default browser:

```bash
smolvm browser open <session_id>
```

## How isolation works

Each sandbox runs inside its own virtual machine, completely separated from your host. On Linux, SmolVM uses [Firecracker](https://firecracker-microvm.github.io/) — lightweight microVMs backed by KVM hardware virtualization. On macOS it uses QEMU.

Unlike containers, which share the host kernel, microVMs give every sandbox its own kernel. An escape would require a hypervisor exploit, not just a kernel vulnerability. You get the convenience of a simple Python API, but every command runs in its own isolated environment.

## Performance

Typical lifecycle timings (p50) on a standard Linux host:

| Phase | Time |
| --- | --- |
| Create \+ Start | ~572ms |
| SSH ready | ~2.1s |
| Command execution | **~43ms** |
| Stop \+ Delete | ~751ms |
| **Full lifecycle (boot, run, teardown)** | **~3.5s** |

<Note>
  Measured on AMD Ryzen 7 7800X3D (8C/16T), Ubuntu Linux, KVM/Firecracker backend.
</Note>

## Get started

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/smolvm/quickstart">
    Set up SmolVM and run your first sandbox in minutes
  </Card>

  <Card title="Installation" icon="download" href="/smolvm/installation">
    Detailed installation instructions for Linux and macOS
  </Card>

  <Card title="AI agent integration" icon="robot" href="/smolvm/guides/ai-agent-integration">
    Plug SmolVM into PydanticAI, OpenAI Agents, LangChain, and more
  </Card>

  <Card title="API reference" icon="book" href="/smolvm/api/smolvm">
    Explore the complete API documentation
  </Card>
</CardGroup>

## Community

- [Slack](https://join.slack.com/t/celestoai/shared_invite/zt-3qc7h8gno-Nb5_PElEWHDNnGqdVzC~4Q)
- [GitHub](https://github.com/CelestoAI/SmolVM)