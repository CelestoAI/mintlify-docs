---
title: "CLI Overview"
description: "Overview of the SmolVM command-line interface and installation"
---

## Overview

The SmolVM CLI provides a set of commands for managing VMs, diagnosing system requirements, and running the dashboard UI. The main entrypoint is the `smolvm` command.

## Installation

Install SmolVM via pip:

```bash  theme={null}
pip install smolvm
```

For dashboard support, install with the `dashboard` extra:

```bash  theme={null}
pip install 'smolvm[dashboard]'
```

## Available commands

The `smolvm` CLI provides the following commands:

| Command           | Description                                       |
| ----------------- | ------------------------------------------------- |
| `smolvm create`   | Create a named SSH-ready VM and leave it running   |
| `smolvm stop`     | Stop a running VM                                  |
| `smolvm list`     | List VMs and their status                          |
| `smolvm ssh`      | SSH into a VM, auto-starting it when needed        |
| `smolvm browser`  | Manage disposable browser sessions                 |
| `smolvm cleanup`  | Clean stale SmolVM resources and VMs               |
| `smolvm doctor`   | Run host diagnostics for the selected backend      |
| `smolvm env`      | Manage environment variables on a running VM       |
| `smolvm ui`       | Start the SmolVM dashboard UI server               |

## Getting help

Display help for the main command:

```bash  theme={null}
smolvm --help
```

Display help for a specific subcommand:

```bash  theme={null}
smolvm <command> --help
```

## Script Aliases

The following script aliases are also available:

* `smolvm-cleanup`: Equivalent to `smolvm cleanup` (standalone cleanup command)

## Next steps

<CardGroup cols={2}>
  <Card title="Create a VM" icon="plus" href="/smolvm/cli/create">
    Provision a named SSH-ready VM
  </Card>

  <Card title="Browser sessions" icon="globe" href="/smolvm/cli/browser">
    Run isolated Chromium sessions with live view
  </Card>

  <Card title="Stop a VM" icon="stop" href="/smolvm/cli/stop">
    Gracefully shut down a running VM
  </Card>

  <Card title="List VMs" icon="list" href="/smolvm/cli/list">
    View all VMs and their status
  </Card>

  <Card title="SSH into a VM" icon="terminal" href="/smolvm/cli/ssh">
    Connect to a VM over SSH
  </Card>

  <Card title="Cleanup" icon="broom" href="/smolvm/cli/cleanup">
    Clean up stale VMs and resources
  </Card>

  <Card title="Doctor" icon="stethoscope" href="/smolvm/cli/doctor">
    Diagnose system requirements
  </Card>

  <Card title="Environment" icon="gear" href="/smolvm/cli/env">
    Manage VM environment variables
  </Card>

  <Card title="UI" icon="window" href="/smolvm/cli/ui">
    Launch the dashboard interface
  </Card>
</CardGroup>
